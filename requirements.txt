# Web framework
fastapi==0.111.0
uvicorn[standard]==0.29.0

# Base de datos
sqlalchemy==2.0.30
alembic==1.13.1
asyncpg==0.29.0
psycopg2-binary==2.9.9

# Validación y configuración
pydantic==2.7.1
pydantic-settings==2.2.1
python-dotenv==1.2.2

# Autenticación y seguridad
# PyJWT reemplaza a python-jose (sin mantenimiento, CVEs sin parche: ver
# auditoría de seguridad -- PYSEC-2024-232/233, PYSEC-2025-185).
PyJWT==2.9.0
bcrypt>=4.0.1
python-multipart==0.0.18

# WebSockets (modo online)
websockets==12.0

# HTTP client (para llamadas a IA externa)
httpx>=0.28.1

# Testing
pytest==8.2.0
pytest-asyncio==0.23.6

# Archivos estáticos (frontend)
aiofiles==23.2.1

# Utilidades
python-dateutil==2.9.0
cloudinary==1.44.1
openpyxl==3.1.5
# Base de datos de husos horarios para zoneinfo (America/Bogota) — no todas
# las imágenes base la traen instalada (ni Windows, ni algunas de Linux
# minimalistas como python:3.11-slim), zoneinfo la necesita para la racha
# diaria y las estadísticas por mes.
tzdata==2024.1

# Procesamiento de imagenes (miniaturas de avatar para el modo Online)
Pillow==10.4.0